<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.koreait.integration.repository.staff">

	<select id="selectAll" resultType="com.koreait.integration.domain.Staff">
		SELECT NO, NAME, DEPART, SALARY, TO_CHAR(REGDATE,'YYYY-MM-DD') AS REGDATE
		  FROM STAFF
	</select>
	
	<select id="selectQuery" resultType="com.koreait.integration.domain.Staff">
		SELECT NO, NAME, DEPART, SALARY, REGDATE
		  FROM STAFF
		 <if test="column != 'BOTH'">WHERE ${column} LIKE '%' || #{query} || '%'</if>
		<if test="column == 'BOTH'">WHERE NAME LIKE '%' || #{query} || '%'
									   OR SALARY LIKE '%' || #{query} || '%'</if>
	</select>


</mapper>

